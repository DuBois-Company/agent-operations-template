"""The example project: a small web application's auth service.

Everything under this directory is fiction. It exists so the harness at
tools/ has a real project to check: a graph with real node ids, a narrative
log that matches it, and acceptance criteria that are commands rather than
adjectives. The code is deliberately small and dependency-free -- the
Python standard library and nothing else -- because its job is to make the
acceptance commands in graph.yaml genuinely pass or genuinely fail.
"""
